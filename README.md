Asqar knows everything !
========================

[Check it out here !](http://www.asqarbegoo.ir)

How to contribute
=================

Install `grunt-cli` via npm
    npm install -g grunt-cli

Then :

    npm install
    grunt serve
    
To deploy the whole project, run `grunt build`.
